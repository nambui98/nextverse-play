import type { NextPage } from 'next';
import { Layout } from 'components/layout'

const Page: NextPage = () => {
  return <Layout>
    <div>this is settings page.</div>
  </Layout>
}

export default Page;